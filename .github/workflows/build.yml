name: Build Legram (Official Bazel 8 Compatibility)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Install Tools
        run: brew install bazelisk

      - name: 🛠 Patch Core (Anti-Delete)
        run: |
          # حقن ميزة منع حذف الرسائل
          grep -rl "case let .deleteMessages(ids):" submodules/ | xargs sed -i '' 's/case let .deleteMessages(ids):/case .deleteMessages(_): return .complete/g' || true

      - name: ⚙️ Official Bazel 8 Legacy Support
        run: |
          # الطريقة الرسمية لتعريف مشروع كـ Workspace في Bazel 8
          # نستخدم ملف .bazelrc لتفعيل الخيارات التي يطلبها النظام في رسائل الخطأ
          echo "common --enable_workspace" > .bazelrc
          echo "common --noenable_bzlmod" >> .bazelrc
          echo "✅ Official compatibility flags set."

      - name: 🔐 Setup Provisioning (Official Structure)
        run: |
          mkdir -p build-input/data/provisioning
          echo "${{ secrets.PROVISIONING_BASE64 }}" | base64 --decode > build-input/data/provisioning/Telegram.mobileprovision
          
          # نسخ الملف لكل الأسماء التي يطلبها مشروع تيليجرام
          cd build-input/data/provisioning
          for name in Share NotificationService NotificationContent Widget Intents SiriIntents BroadcastUpload ShareExtension NotificationServiceExtension; do
            cp Telegram.mobileprovision $name.mobileprovision
          done
          
          # إنشاء ملف BUILD الرسمي داخل المجلد الفرعي (لأن الخطأ كان يبحث داخل provisioning)
          cat <<EOF > BUILD
          package(default_visibility = ["//visibility:public"])
          exports_files(glob(["*.mobileprovision"]))
          EOF
          cd ../../../

      - name: 🏗 Register Local Repository Officially
        run: |
          # الطريقة الرسمية لربط المجلدات في نظام WORKSPACE
          # سنضيف التعريف في نهاية ملف WORKSPACE الأصلي
          echo "" >> WORKSPACE
          echo 'local_repository(name = "build_configuration", path = "build-input/data")' >> WORKSPACE

      - name: 🔑 Install Signing Certificate
        run: |
          echo "${{ secrets.P12_BASE64 }}" | base64 --decode > certificate.p12
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          security create-keychain -p "" "$KEYCHAIN_PATH"
          security import certificate.p12 -k "$KEYCHAIN_PATH" -P "${{ secrets.P12_PASSWORD }}" -A
          security list-keychain -d user -s "$KEYCHAIN_PATH"

      - name: ⚙️ Setup Config JSON
        run: |
          cat <<EOF > build-input/data/config.json
          {
            "bundle_id": "com.CommitteeSupervision.gov",
            "api_id": "${{ secrets.TELEGRAM_API_ID }}",
            "api_hash": "${{ secrets.TELEGRAM_API_HASH }}",
            "team_id": "9NS9KBTAFV",
            "app_center_id": "0",
            "is_internal_build": false,
            "is_appstore_build": false,
            "appstore_id": "0",
            "app_specific_url_scheme": "legram",
            "premium_iap_product_id": "com.legram.premium",
            "enable_siri": false,
            "enable_icloud": false
          }
          EOF

      - name: 🚀 Start Official Build
        run: |
          python3 build-system/Make/Make.py \
            --overrideXcodeVersion \
            --overrideBazelVersion \
            build \
            --buildNumber 1 \
            --configurationPath build-input/data/config.json \
            --configuration release_arm64 \
            --xcodeManagedCodesigning

      - name: 📦 Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Legram-Official-Build
          path: build/artifacts/*.ipa