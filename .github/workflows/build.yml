name: Legram Final Build (The 6.4.0 Foundation)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: 🚀 Force Bazel 6.4.0 Installation
        run: |
          mkdir -p build-input
          # تنزيل نسخة 6.4.0 المخصصة لـ Apple Silicon (arm64) لأن macos-latest هو M1
          curl -L https://github.com/bazelbuild/bazel/releases/download/6.4.0/bazel-6.4.0-darwin-arm64 -o build-input/bazel-custom
          chmod +x build-input/bazel-custom
          # التأكد من النسخة
          ./build-input/bazel-custom --version

      - name: ⚙️ Reset Build Environment
        run: |
          # تنظيف أي إعدادات سابقة تسببت في مشاكل Bazel 8
          rm -f .bazelrc.user
          git checkout .bazelrc || true
          # إجبار bazelisk (إن وُجد) على استخدام 6.4.0
          echo "6.4.0" > .bazelversion

      - name: 🏗 Prepare Official Configuration
        run: |
          mkdir -p build-input/data/provisioning
          
          # ملف config.json الكامل والشامل (للقضاء على أي KeyError نهائياً)
          cat <<EOF > build-input/data/config.json
          {
            "bundle_id": "com.CommitteeSupervision.gov",
            "api_id": "${{ secrets.TELEGRAM_API_ID }}",
            "api_hash": "${{ secrets.TELEGRAM_API_HASH }}",
            "team_id": "9NS9KBTAFV",
            "app_center_id": "0",
            "is_internal_build": false,
            "is_appstore_build": false,
            "appstore_id": "0",
            "app_specific_url_scheme": "legram",
            "premium_iap_product_id": "com.legram.premium",
            "enable_siri": false,
            "enable_icloud": false,
            "sg_config": ""
          }
          EOF

          # ملف variables.bzl
          cat <<EOF > build-input/data/variables.bzl
          bundle_id = "com.CommitteeSupervision.gov"
          api_id = "${{ secrets.TELEGRAM_API_ID }}"
          api_hash = "${{ secrets.TELEGRAM_API_HASH }}"
          team_id = "9NS9KBTAFV"
          version = "11.5.1"
          build_number = "1"
          enable_siri = false
          enable_icloud = false
          EOF

          echo 'exports_files(["variables.bzl", "config.json"])' > build-input/data/BUILD

      - name: 🔐 Setup Provisioning & Identity
        run: |
          echo "${{ secrets.PROVISIONING_BASE64 }}" | base64 --decode > build-input/data/provisioning/Telegram.mobileprovision
          cd build-input/data/provisioning
          for name in Telegram Share NotificationService NotificationContent Widget Intents SiriIntents BroadcastUpload ShareExtension NotificationServiceExtension; do
            cp Telegram.mobileprovision \$name.mobileprovision
          done
          echo 'package(default_visibility = ["//visibility:public"])' > BUILD
          echo 'exports_files(glob(["*.mobileprovision"]))' >> BUILD

      - name: 🔗 Inject Stable WORKSPACE
        run: |
          # إضافة تعريف المستودع في أول سطر لضمان الاستقرار في Bazel 6
          sed -i '' '1i\
          local_repository(name = "build_configuration", path = "build-input/data")\
          ' WORKSPACE

      - name: 🔑 Import Certificate
        run: |
          echo "${{ secrets.P12_BASE64 }}" | base64 --decode > certificate.p12
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          security create-keychain -p "" "$KEYCHAIN_PATH"
          security import certificate.p12 -k "$KEYCHAIN_PATH" -P "${{ secrets.P12_PASSWORD }}" -A
          security list-keychain -d user -s "$KEYCHAIN_PATH"

      - name: 🛠 Patch Core
        run: |
          grep -rl "case let .deleteMessages(ids):" submodules/ | xargs sed -i '' 's/case let .deleteMessages(ids):/case .deleteMessages(_): return .complete/g' || true

      - name: 🚀 The Final Build (Executing Bazel 6.4.0)
        run: |
          # نستخدم --bazel لتمرير مسار الإصدار 6.4.0 الذي قمنا بتنزيله
          # ونستخدم --overrideBazelVersion لتجاهل التحقق من الإصدار
          python3 build-system/Make/Make.py \
            --bazel=$(pwd)/build-input/bazel-custom \
            --overrideXcodeVersion \
            --overrideBazelVersion \
            build \
            --buildNumber 1 \
            --configurationPath build-input/data/config.json \
            --configuration release_arm64 \
            --xcodeManagedCodesigning

      - name: 📦 Archive Product
        uses: actions/upload-artifact@v4
        with:
          name: Legram-Final-IPA
          path: build/artifacts/*.ipa