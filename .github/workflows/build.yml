name: Build Legram (Clean Build)

on:
  workflow_dispatch: # للتشغيل اليدوي من قبلك

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # الآن سيقوم السيرفر بتحميل كل شيء تلقائياً لأن الروابط أصبحت صحيحة
          submodules: recursive
          fetch-depth: 1

      - name: Install Bazel
        run: brew install bazelisk

      - name: Prepare API Keys
        run: |
          mkdir -p build-input/data
          echo "api_id = ${{ secrets.TELEGRAM_API_ID }}" > build_variables.conf
          echo "api_hash = ${{ secrets.TELEGRAM_API_HASH }}" >> build_variables.conf

      - name: Build Legram IPA
        run: |
          python3 build-system/Make/Make.py \
            --bundle_id "com.legram.ios" \
            --display_name "Legram" \
            --api_id "${{ secrets.TELEGRAM_API_ID }}" \
            --api_hash "${{ secrets.TELEGRAM_API_HASH }}" \
            build

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Legram-Package
          path: build/artifacts/*.ipa